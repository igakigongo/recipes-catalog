import React from 'react';

const NoRecipesFound = () => (
  <div className="container-fluid">
    <div className="alert alert-info">
      Information:
      <span className="mx-2 font-weight-bold">No recipes found.</span>
    </div>
  </div>
);

export default NoRecipesFound;
